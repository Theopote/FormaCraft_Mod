package com.formacraft.common.component.socket;

/**
 * AlignmentPolicy（对齐策略）：非常建筑感。
 */
public enum AlignmentPolicy {
    /** 居中（窗、门） */
    CENTER,

    /** 底部对齐（门、壁龛） */
    BOTTOM,

    /** 顶部对齐（飞檐、雨棚） */
    TOP,

    /** 边缘对齐（栏杆） */
    EDGE
}
