# FormaCraft

A Fabric mod that uses natural language to generate buildings in Minecraft.

## Development
- Requires Java 21, Fabric Loom, Yarn mappings.
- Build: `./gradlew build`
- Run Client: `./gradlew runClient`
- Run Server: `./gradlew runServer`
